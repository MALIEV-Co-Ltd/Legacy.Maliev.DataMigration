# Dormant post-export template only. It may be rendered after the authenticated
# exact-24 snapshot manifest exists. Applying it remains a separately authorized action.
apiVersion: batch/v1
kind: Job
metadata:
  name: legacy-exact24-cleanup-__RUN_ID__
  namespace: maliev-legacy
  labels:
    app.kubernetes.io/name: legacy-exact24-shadow-cleanup
    app.kubernetes.io/managed-by: legacy-maliev-data-migration
    maliev.com/database-purpose: legacy-shadow
spec:
  backoffLimit: 0
  activeDeadlineSeconds: 3600
  ttlSecondsAfterFinished: 86400
  template:
    metadata:
      labels:
        app.kubernetes.io/name: legacy-exact24-shadow-cleanup
        app.kubernetes.io/managed-by: legacy-maliev-data-migration
    spec:
      serviceAccountName: legacy-data-migration-shadow-provisioner
      automountServiceAccountToken: false
      restartPolicy: Never
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        fsGroup: 65532
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile:
          type: RuntimeDefault
      initContainers:
        - name: stage-owner-protected-cleanup-material
          image: __STAGER_IMAGE_DIGEST__
          imagePullPolicy: IfNotPresent
          command:
            - /bin/sh
            - -ec
            - |
              umask 077
              cp /source/authorization/authorization-private.pem /staged/authorization/authorization-private.pem
              cp /source/execution/execution-private.pem /staged/execution/execution-private.pem
              cp /source/snapshot/snapshot-root.key /staged/snapshot/snapshot-root.key
              chmod 600 /staged/authorization/authorization-private.pem /staged/execution/execution-private.pem /staged/snapshot/snapshot-root.key
          securityContext: &stagerSecurityContext
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 65532
            runAsGroup: 65532
            capabilities:
              drop: ["ALL"]
          resources:
            requests:
              cpu: 5m
              memory: 16Mi
            limits:
              cpu: 100m
              memory: 24Mi
          volumeMounts:
            - name: authorization-signing-source
              mountPath: /source/authorization
              readOnly: true
            - name: execution-signing-source
              mountPath: /source/execution
              readOnly: true
            - name: snapshot-source
              mountPath: /source/snapshot
              readOnly: true
            - name: authorization-signing-material
              mountPath: /staged/authorization
            - name: execution-signing-material
              mountPath: /staged/execution
            - name: snapshot-material
              mountPath: /staged/snapshot
        - name: authorize-reviewed-cleanup
          image: __RUNNER_IMAGE_DIGEST__
          imagePullPolicy: IfNotPresent
          args: ["authorize-cleanup", "--config", "/run/legacy-migration/run-config.json"]
          env:
            - name: LEGACY_DEPLOY_ENABLED
              value: "false"
            - name: LEGACY_MIGRATION_AUTHORIZATION_SIGNING_KEY_FILE
              value: /run/legacy-migration/signing/authorization-private.pem
            - name: LEGACY_MIGRATION_SNAPSHOT_ENCRYPTION_KEY_FILE
              value: /run/legacy-migration/snapshot/snapshot-root.key
          securityContext: &cleanupSecurityContext
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 65532
            runAsGroup: 65532
            capabilities:
              drop: ["ALL"]
          resources: &cleanupResources
            requests:
              cpu: 10m
              memory: 32Mi
            limits:
              cpu: "1"
              memory: 1Gi
          volumeMounts:
            - name: run-artifacts
              mountPath: /run/legacy-migration
            - name: authorization-signing-material
              mountPath: /run/legacy-migration/signing
              readOnly: true
            - name: snapshot-material
              mountPath: /run/legacy-migration/snapshot
              readOnly: true
            - name: kubernetes-api-access
              mountPath: /var/run/secrets/kubernetes.io/serviceaccount
              readOnly: true
      containers:
        - name: delete-exported-run-owned-shadows
          image: __RUNNER_IMAGE_DIGEST__
          imagePullPolicy: IfNotPresent
          args: ["cleanup-shadows", "--config", "/run/legacy-migration/run-config.json"]
          env:
            - name: LEGACY_DEPLOY_ENABLED
              value: "false"
            - name: LEGACY_MIGRATION_POSTGRES_ADMIN_CONNECTION
              valueFrom:
                secretKeyRef:
                  name: __RUNTIME_SECRET_NAME__
                  key: postgres-shadow-connection
            - name: LEGACY_MIGRATION_EXECUTION_SIGNING_KEY_FILE
              value: /run/legacy-migration/signing/execution-private.pem
            - name: LEGACY_MIGRATION_SNAPSHOT_ENCRYPTION_KEY_FILE
              value: /run/legacy-migration/snapshot/snapshot-root.key
          securityContext: *cleanupSecurityContext
          resources: *cleanupResources
          volumeMounts:
            - name: run-artifacts
              mountPath: /run/legacy-migration
            - name: execution-signing-material
              mountPath: /run/legacy-migration/signing
              readOnly: true
            - name: snapshot-material
              mountPath: /run/legacy-migration/snapshot
              readOnly: true
            - name: kubernetes-api-access
              mountPath: /var/run/secrets/kubernetes.io/serviceaccount
              readOnly: true
      volumes:
        - name: run-artifacts
          persistentVolumeClaim:
            claimName: __RUN_ARTIFACTS_PVC_NAME__
        - name: authorization-signing-source
          secret:
            secretName: __SIGNING_SECRET_NAME__
            defaultMode: 288
            items:
              - key: authorization-private.pem
                path: authorization-private.pem
                mode: 288
        - name: execution-signing-source
          secret:
            secretName: __SIGNING_SECRET_NAME__
            defaultMode: 288
            items:
              - key: execution-private.pem
                path: execution-private.pem
                mode: 288
        - name: snapshot-source
          secret:
            secretName: __SNAPSHOT_SECRET_NAME__
            defaultMode: 288
            items:
              - key: snapshot-root.key
                path: snapshot-root.key
                mode: 288
        - name: authorization-signing-material
          emptyDir:
            medium: Memory
            sizeLimit: 1Mi
        - name: execution-signing-material
          emptyDir:
            medium: Memory
            sizeLimit: 1Mi
        - name: snapshot-material
          emptyDir:
            medium: Memory
            sizeLimit: 1Mi
        - name: kubernetes-api-access
          projected:
            defaultMode: 288
            sources:
              - serviceAccountToken:
                  path: token
                  expirationSeconds: 600
              - configMap:
                  name: kube-root-ca.crt
                  items:
                    - key: ca.crt
                      path: ca.crt
                      mode: 288
